{
  "_comment": "Deferred / planned work for GABES. Basics & current behaviour live in README.md; this file tracks things intentionally NOT done yet. Each item: {id, area, title, why, status}. status: deferred | in_progress | done.",
  "_daily_report_sync": "Daily reports through 2026-07-09 have been reflected in this checklist.",
  "_priority_policy": "Review/checklist priority uses P1/P2/P3 going forward. P1 blocks correctness, reproducibility, release, or user-facing trust. P2 is important and should be fixed before publication/release or broad reuse, but does not currently break tests. P3 is housekeeping, polish, or low-risk maintainability. This priority is separate from GROUP B/C implementation burden.",
  "_group_policy": "GROUP B items are safe but substantial coding work. GROUP C items are heavy / need design alignment; ask the user before coding any GROUP C item.",
  "items": [
    {
      "id": "rydberg-power-to-rabi",
      "area": "schemes/rydberg.py",
      "title": "Connect Rydberg probe/coupling beam power to the OBE drive",
      "why": "DONE. coupling_power_mw / beam_diameter_mm now drive Ω_c via the FWM-style √(P/d²) intensity scaling (RydbergEITScheme._coupling_rabi), anchored so the reference operating point reproduces the fitted coupling_rabi_mhz (kept as an Advanced anchor) -- no absolute 480 nm dipole needed. probe_power_uw also drives the weak Ω_P through the same anchored √P/d scaling, so probe-power broadening is represented. These knobs are recompute=True solve knobs. Also landed alongside: residual two-photon Doppler option (per-level k via atoms.doppler_ratios), 2-channel dephasing (5S-40D vs 40D-39F), AT center-shift metric, cached probe-line constants, finite-IF static discriminator proxy, and opt-in temperature-linked dephasing. cache/defaults bumped through rydberg-eit-v4.",
      "status": "done"
    },
    {
      "id": "biphoton-predictive-model",
      "area": "schemes/fwm.py + observables.py",
      "title": "Replace calibrated biphoton estimate with a predictive model",
      "why": "PARTIAL (biphoton_model toggle, Predictive is the default). The biphoton WAVEFORM is now solved from first principles: Doppler-averaged cascade amplitude (Kim QST 9,045006 (2024) Eq.2; Du JOSAB 25,C98 (2008) Eq.15; Chen PRR 4,023132 (2022) Eq.3-5) with the Omega_c^2 Autler-Townes term in the two-photon denominator (not a weak-coupling drive), the collective velocity-class coherent sum, natural-linewidth decay (no injected emission_decay_ns), a waveform-derived source bandwidth, and the wavelength-dependent BTW width ORDERING (852-917 nm narrower than 852-795 nm). g2_SI(tau) is computed from |psi|^2 with physical accidentals (no added-accidental forcing in Predictive mode). STILL ANCHORED / APPROXIMATE: (a) absolute pair rate is reference-anchored because the lumped 4-level model does not pin the collection coefficient C; its current scalar law uses pump power, coupling drive, vector phase matching, and sqrt(N/N_ref), while OD/cell length do not directly scale the rate; (b) absolute ns-widths now CONVERGE numerically: the velocity-class coherent sum integrates a narrow ~Gamma/k source resonance (~17 m/s for the telecom cascade) that a fixed Doppler-sized velocity step aliased, giving a factor-~20 non-monotonic FWHM swing vs biphoton_velocity_step; GenericFWMSolver._converged_predictive_waveform now auto-refines the velocity grid (oversample the measured resonance, halve until the |psi|^2 FWHM is stable, PRED_V_* constants) so the predictive telecom source BTW converges to ~0.17 ns; biphoton_stats now reports that intrinsic width separately from the detector-timing-broadened width (~0.50 ns with the default 0.55 ns net timing-difference response, compared with the measured 0.56(4) ns) while preserving fwhm_ns as a detected-width compatibility alias, and a cap-hit case is surfaced as 'unconverged / qualitative only' in the validation table. The wavelength ORDERING is physical, but exact per-source widths and the Cs ratio still need topology-specific Rabi/dephasing calibration; (c) OD waveform reshaping (Du/Chen rho-bar, GenericFWMSolver._apply_longitudinal_response) is implemented but OFF by default (param biphoton_od_reshaping) -- the lumped model overestimates high-OD group delay; (d) full quantum-Langevin noise (Kolchin PRA 75,033814 (2007)) is not modelled. Next: per-source Rabi/dephasing calibration for exact widths, and a Langevin noise channel.",
      "status": "in_progress"
    },
    {
      "id": "buffer-gas-pressure-shift",
      "area": "constants.py",
      "title": "Promote Ne buffer broadening to a gas/species/line table with pressure shift + Dicke narrowing",
      "why": "GROUP B. Safe but substantial cross-scheme work. neon_buffer_broadening() is a fixed FWHM-per-Torr line only; pressure shift and Dicke narrowing are not modelled (see constants.py TODO). Generalise to a gas/species/line coefficient table and apply low-order pressure shift + phenomenological Dicke narrowing in OD/SAS, Magneto, and Lambda without changing solver dimensions. Do not include full velocity-changing collisions in this item.",
      "status": "deferred"
    },
    {
      "id": "figureless-observables-paths",
      "area": "schemes/* + streamlit_app.py",
      "title": "Split metric/table readout from Matplotlib figure generation across schemes",
      "why": "DONE for the current scheme API. Scheme.headless_observables now provides the public hook and centralized wrapper: schemes opt in by exposing observables(..., include_figures=False), while the base class performs the common headless dispatch. All current user-facing schemes (OD/SAS, Lambda, Rydberg-EIT, Magneto, FWM) plus the internal OD validation primitive support this path so metrics/tables can skip Matplotlib while preserving the existing UI figure path. tests/test_headless_observables.py proves the headless path does not call plt.subplots and that the base wrapper passes include_figures=False without forbidding future custom overrides; tests/test_schemes_render.py still proves normal figure rendering. No existing non-UI batch/report callsite needed rewiring; future batch/report callers should use headless_observables.",
      "status": "done"
    },
    {
      "id": "paper-data-availability-claim",
      "area": "docs/squeezing_report/squeezing_paper.tex + README.md",
      "title": "Align paper data-availability language with actual repository visibility",
      "priority": "P2",
      "why": "P2 DONE and reverified 2026-07-17. The unauthenticated GitHub repository API reports Shake2313/fwm-squeezing-app as public (private=false, visibility=public). README.md and docs/Userguide/GABES_User_Guide_v2.html now say public, matching the paper. The paper also distinguishes retained raw scan archives from the rendered-only Stage-I--II figures whose raw grids were overwritten, so it no longer claims that every figure has archived raw data. tests/test_docs_consistency.py pins the local wording and archive-scope caveat; if repository visibility changes, update all three documents together.",
      "status": "done"
    },
    {
      "id": "paper-zeeman-scope-wording",
      "area": "docs/squeezing_report/squeezing_paper.tex",
      "title": "Clarify that FWM Zeeman structure is CG diagnostic/bookkeeping, not a full Zeeman Floquet solve",
      "priority": "P2",
      "why": "P2 DONE and reverified 2026-07-17. The paper and app now distinguish hyperfine CG-weighted lumped couplings from the 24-level Zeeman CG-sum diagnostic: the diagnostic is reported but not multiplied into the main solve, and the full Zeeman Floquet solve remains deferred. tests/test_docs_consistency.py and tests/test_fwm_generic.py pin the wording boundary.",
      "status": "done"
    },
    {
      "id": "latex-build-artifact-ignore",
      "area": ".gitignore + docs/squeezing_report",
      "title": "Keep LaTeX auxiliary build artifacts out of version control",
      "priority": "P3",
      "why": "P3. The 2026-07-09 review flagged .aux/.out/.toc files as low-risk build artifacts. .gitignore now excludes LaTeX auxiliary outputs; source .tex, figure pipeline, archived data, rendered figures, and the paper PDF can still be tracked intentionally.",
      "status": "done"
    },
    {
      "id": "magneto-buffer-relaxation-map",
      "area": "schemes/magneto.py",
      "title": "Map Ne buffer pressure to default ground relaxation and depolarization",
      "why": "GROUP B. Safe but needs empirical defaults and tests. Today ne_pressure_torr mainly affects optical broadening while buffer_ground_relax_khz and collisional_depol_khz remain independent overrides. Add a simple table/experience-based default mapping from pressure to diffusion-limited ground relaxation and spin-depolarization, with Advanced overrides left in place.",
      "status": "deferred"
    },
    {
      "id": "fwm-per-source-biphoton-calibration",
      "area": "schemes/fwm.py",
      "title": "Add per-source Rabi/dephasing/width calibration for predictive biphoton modes",
      "why": "GROUP B. Safe for runtime because it should adjust scalar / low-dimensional topology parameters, not solver dimension. Separate topology-specific effective linewidth, dephasing, and coupling-scale anchors so predictive biphoton widths can be reported as calibrated per source rather than one lumped approximation.",
      "status": "deferred"
    },
    {
      "id": "fwm-line-strength-factorization",
      "area": "schemes/fwm.py",
      "title": "Factor seeded FWM line_strength into lab-facing residual components",
      "why": "DONE. SeededCouplingFactors now separates the backward-compatible reference residual from explicit one-sided effective-coupling penalties for unresolved transverse mode mismatch, extra polarization-channel leakage, and reduced Zeeman-path participation relative to the anchored setup. The reference anchor remains 0.74 and all three penalties default to 1.0, so the legacy direct API and app reference arrays remain bit-identical; the code deliberately does not invent an unsupported numerical decomposition of 0.74. Raw results and the Derived table expose the anchor, each penalty, their penalty product, the combined residual, and the final first-principles-normalized coupling. Help/docs state that these are phenomenological penalties rather than raw power/purity measurements, that the Zeeman penalty does not enable a full manifold solve, and that mode_overlap_penalty is separate from Ultra's normalized axial crossing-angle profile. Regression tests pin legacy equivalence, multiplication, defaults, ranges, and the double-counting warning.",
      "status": "done"
    },
    {
      "id": "lambda-residual-two-photon-doppler",
      "area": "schemes/absorption.py",
      "title": "Add Lambda residual two-photon Doppler / beam-angle mismatch option",
      "why": "DONE. Lambda now exposes an Advanced beam_angle_mrad knob that defaults to 0 mrad, preserving the co-propagating Doppler-free two-photon resonance. Nonzero mismatch computes a residual |Delta k|/k with the shared gabes.beam geometry helper and carries it through atoms.lambda3(two_photon_doppler_ratio=...) / AtomModel.S_v, so warm-vapor EIT/CPT broadens or washes out without increasing solver dimension or adding a full velocity-changing-collision model. The same work extracted anchored Rabi scaling, transit broadening, wave-vector, and residual-k helpers to gabes/beam.py and refactored Lambda, Rydberg-EIT, and generic FWM users onto them.",
      "status": "done"
    },
    {
      "id": "low-order-polarization-zeeman-proxies",
      "area": "schemes/sas.py + schemes/rydberg.py",
      "title": "Add low-order polarization / Zeeman proxy knobs without full manifolds",
      "why": "GROUP B. Safe but model-design work. For OD/SAS and Rydberg, add optional low-dimensional proxies such as effective branching selectors, polarization purity, or uncompensated asymmetry broadening to explain common lab distortions without adopting a full Zeeman manifold.",
      "status": "deferred"
    },
    {
      "id": "fwm-batch-cache-and-map-separation",
      "area": "schemes/fwm.py",
      "title": "Tighten FWM batch caches and separate expensive phase-map views",
      "why": "DONE for the low-risk cache/view scope. Topology specs and GenericFWMSolver instances retain bounded scalar-key LRU caches; seeded FWM now builds the shared Doppler interpolation geometry once for all four susceptibility tables, and the predictive biphoton path no longer constructs a user-step velocity grid that auto-refinement immediately discarded. The optional 121x121 signal-idler phase-matching map was removed from the source solve and moved to figure-only observables; phase_detail is now navigate-only (recompute=False), so headless/batch readout skips the map and changing Balanced/Fine does not rerun the waveform solve. Mutable public velocity/scan ndarrays are deliberately not globally memoized: the scheme compute cache already covers identical solves, while shared mutable array returns or rounded float keys would add correctness risk for negligible gain. Tests pin one interpolation-weight build per seeded solve, bit-identical Balanced/Fine source outputs, lazy headless behavior, and the unchanged rendered Fine map.",
      "status": "done"
    },
    {
      "id": "full-velocity-changing-collision-kernel",
      "area": "doppler.py + schemes/sas.py + schemes/magneto.py + schemes/absorption.py",
      "title": "Full velocity-changing-collision model across velocity classes",
      "why": "GROUP C. Heavy / needs design alignment. This couples velocity classes and breaks the current separable Maxwell-weight averaging, likely increasing matrix dimension or requiring iterative solves. Ask the user for scope and modelling choices before coding.",
      "status": "deferred"
    },
    {
      "id": "lambda-hyperfine-resolved-manifold",
      "area": "schemes/absorption.py + species.py + zeeman.py",
      "title": "Hyperfine-resolved Lambda manifold with optical pumping",
      "why": "GROUP C. Heavy / needs design alignment. Moving from the compact 3-level Lambda model to hyperfine/possibly Zeeman-resolved manifolds greatly increases density-matrix size and changes the interpretation of all Lambda presets. Ask the user before coding.",
      "status": "deferred"
    },
    {
      "id": "fwm-quantum-langevin-noise",
      "area": "schemes/fwm.py + observables.py",
      "title": "Full quantum-Langevin source-noise channel for FWM",
      "why": "GROUP C. Heavy / needs design alignment. This changes the source-noise model beyond waveform + detector accidentals and likely adds correlation/noise-propagation state. Ask the user for modelling scope before coding.",
      "status": "deferred"
    },
    {
      "id": "fwm-full-zeeman-floquet-solve",
      "area": "schemes/fwm.py + zeeman.py",
      "title": "Full 24-level Zeeman / Floquet solve for FWM",
      "why": "GROUP C. Heavy / needs design alignment. The current 24-level Zeeman manifold is diagnostic/correction bookkeeping, not the main solve. Promoting it to a full solve changes model complexity, runtime, and validation surface. Ask the user before coding.",
      "status": "deferred"
    },
    {
      "id": "rydberg-full-zeeman-polarization-field-model",
      "area": "schemes/rydberg.py + zeeman.py",
      "title": "Full Rydberg Zeeman / polarization / stray-field / ionization model",
      "why": "GROUP C. Heavy / needs design alignment. Absolute electrometry calibration would need Zeeman sublevels, polarization selection rules, stray E/B mixing, state-selective ionization/dephasing, and optical-pumping redistribution. This goes well beyond the lumped 4-level ladder. Ask the user before coding.",
      "status": "deferred"
    },
    {
      "id": "full-time-domain-superhet-demodulation",
      "area": "schemes/rydberg.py + observables.py",
      "title": "Full time-domain superheterodyne / lock-in demodulation chain",
      "why": "GROUP C. Heavy / needs design alignment. Static IF/slope proxies are safe, but a real detector/lock-in/superheterodyne chain introduces separate time-domain signal processing and noise assumptions. Ask the user for the desired readout model before coding.",
      "status": "deferred"
    },
    {
      "id": "sabes-layout-model",
      "area": "sabes/layout/ (new) + sabes/beamline.py",
      "title": "Position-aware layout model that drives the physics",
      "why": "GROUP B. DONE. sabes/layout/ holds two coordinate systems that never mix: `display` (Node.x/y/angle, Link.via) draws the table, `path_m` does the physics, and neither is derived from the other. A test pins that separation by showing the metres-per-display-unit ratio spans more than an order of magnitude across links -- a schematic has no single scale, which is precisely why reading distances off the drawing would have been wrong. Detection now asks the layout for cell->D-mirror and cell->lens instead of carrying `cell_to_dmirror_m` and `pd_lens_distance_m` as coefficients (both removed), and the crossing angle derives from the same measured 23 inch. beam_at(layout, x, y, chain) returns the beam at a point with its mode propagated TO that point, which is what makes a beam profiler on a long arm mean anything; it is the single input every virtual instrument will take. Two bugs were caught building it: stages were keyed by name alone, so the pump and seed arms -- both of which have a stage called 'at cell' -- collided and a probe on the pump reported the seed's power; and `Telescope.apply` collapsed the pair into one step, so a probe between L1 and L2 was handed the state from after the telescope (the beamline now records the intermediate). Results are unchanged, and the bridge in fact now reproduces a hand-driven GABES run to exactly zero at both Fast and Ultra. Still nominal, and worth measuring: D-mirror->iris (100 mm) and iris->lens (216 mm), which together set the photodiode spot.",
      "priority": "P1",
      "status": "done"
    },
    {
      "id": "sabes-beamline-order-corrections",
      "area": "sabes/beamline.py",
      "title": "Reconcile the seed path with the part 1 setup drawing",
      "why": "DONE, as part of sabes-layout-model. The part 1 drawing's Glan-Taylor after the etalon chain, the half-wave plate between etalons 1 and 2, and the distinct collimators (F230 pump, F220 seed) are all in the layout, along with the elements the physics still does not model but the table has: optical isolators, the MOPA's cylindrical lens, the seed-path noise eater, and the Rb-natural locking arm. Those are drawn and inert -- `Node.clickable` is true exactly when the physics owns a parameter, so 'clickable' and 'modelled' cannot drift apart. A test asserts every SetupSettings and DetectionSettings knob is owned by some optic, which is what Stage D needs in order to empty the sidebar without orphaning anything.",
      "priority": "P2",
      "status": "done"
    },
    {
      "id": "sabes-canvas-component-spike",
      "area": "sabes/components/ (new)",
      "title": "Bidirectional Streamlit canvas component: click -> Python",
      "why": "GROUP B. DONE, and the fallback is not needed. A hand-written bidirectional component works: sabes/components/canvas.py declares the component over a static frontend/index.html that implements the postMessage protocol (streamlit:componentReady / setFrameHeight / setComponentValue) in vanilla JS, with no npm, no lockfile and no build step -- it ships as package data. Verified against the running app at ?app=sabes&dev=canvas: the component loads and sizes its own frame; clicking an optic returns its id; clicking empty table returns a coordinate in SPEC units, checked at three different render scales including 0.42x, where a naive screen-pixel reading would have been wrong by more than a factor of two; and a repeat click on the same optic still re-fires, because Streamlit reruns only when a component value CHANGES and every event therefore carries a monotonic seq. One real bug was caught and fixed on the way: exporting the drawing function as `canvas` from the package __init__ shadowed the submodule of the same name, so it is now `svg_canvas`. Remaining before Stage C: confirm on Streamlit Community Cloud, since proving it on a laptop was never the point. Delete sabes/components/spike.py and its ?dev=canvas route once the table view lands.",
      "priority": "P1",
      "status": "done"
    },
    {
      "id": "sabes-optical-table-view",
      "area": "sabes_page.py + sabes/layout/",
      "title": "Optical-table view reproducing the two setup drawings",
      "why": "GROUP B. Needs sabes-layout-model and sabes-canvas-component-spike. Two tabs matching references/IDS setup part 1.png and part 2.png. Every optic is drawn, including ones the physics does not model (isolators, steering mirrors, beam blocks), but only modelled optics are interactive -- so 'clickable' and 'in the physics' mean the same thing. Beam thickness CANNOT be to scale (530 um on a 1 m table is 0.05 percent of the width): it is a deliberate log-scaled visual encoding, and the legend has to say so. Path colours follow the drawings' own convention: orange pump, red dashed seed, blue conjugate.",
      "priority": "P2",
      "status": "deferred"
    },
    {
      "id": "sabes-optic-popup-editors",
      "area": "sabes_page.py",
      "title": "Click an optic to edit its parameters in a dialog",
      "why": "GROUP B. Needs sabes-optical-table-view. This -- not the drawing -- is what actually fixes the parameter-count problem: 23 sidebar controls in 7 groups migrate into per-optic popups, leaving roughly five global controls (fidelity, Solve knobs, Reset, instrument selector, part tabs). st.dialog is native in the pinned Streamlit, so the work is hit-testing and migration, not the modal. Side benefit: per-etalon detuning becomes possible -- the model already accepts a 3-tuple and only the UI was collapsing it, while in the lab each stage has its own knob and temperature.",
      "priority": "P2",
      "status": "deferred"
    },
    {
      "id": "sabes-virtual-instruments",
      "area": "sabes/instruments/ (new)",
      "title": "Virtual lab instruments driven by the real model",
      "why": "GROUP B. Needs sabes-layout-model for beam_at(); needs the table view only for placement. Kept free of layout and UI dependencies so the package is reusable: input is a BeamState or a photocurrent signal, output is a Reading. Power meter, beam profiler, wavemeter and photodiode take an optical head placed on the table; the oscilloscope and spectrum analyser take a photodiode as their head, as in the lab. Priority is the spectrum analyser (it is the instrument that measures squeezing, so it reproduces the paper's Fig. 3/4) and the wavemeter (dropped either side of the etalon chain it shows the filtering in one picture). Suggested first demo: a photodiode plus oscilloscope on the part 1 Rb-natural reference arm, showing the SAS trace the ECDL is locked to -- GABES already has an SAS scheme, so the physics is free.",
      "priority": "P2",
      "status": "deferred"
    },
    {
      "id": "sabes-instrument-trace-honesty",
      "area": "sabes/instruments/oscilloscope.py + spectrum_analyzer.py",
      "title": "Label synthesised instrument traces as synthesised",
      "why": "Cheap, and load-bearing for trust. The model is steady-state: there is no stochastic time evolution and no quantum Langevin noise (see fwm-quantum-langevin-noise). The oscilloscope's scan mode is a swept-laser trace, which is genuinely what a scope shows here and carries no extra assumptions -- that is the default. Its time-series mode samples from the modelled noise PSD, and the spectrum analyser is constructed from the noise budget (shot noise, squeezed level, electronic floor) rather than simulated. Both re-express what is already known instead of deriving anything new, and both must carry that label ON SCREEN: a plot that looks like instrument output invites being read as evidence.",
      "priority": "P1",
      "status": "deferred"
    },
    {
      "id": "sabes-calibration-measurements",
      "area": "sabes/calibration_sim2025.json",
      "title": "Replace the placeholder coefficients with measurements",
      "why": "Blocked on lab data rather than on code. 25 of 42 coefficients are unverified (nominal or lab), so results that depend on them are predicted, not reproduced. The three that matter most: (1) the EOM V_pi -- currently back-solved jointly with a 5.3 dB insertion loss to reproduce the reported ~10 percent minus-first-order efficiency, so the two coefficients are entangled and one sideband-ratio-versus-RF-power sweep frees both; (2) the tapered-amplifier P-I curve, which fixes threshold/slope/saturation from one measurement at known seed power; (3) the detector NEP -- the 3.3 pW/rtHz figure is the PDB450A spec with its STOCK photodiode, and this unit has S3883 diodes fitted, which changes junction capacitance and therefore the amplifier noise gain. Every clearance number currently rests on that stock figure. A measured etalon transmission scan would additionally replace the etalon_leak floor, which is presently fitted to a single published number.",
      "priority": "P2",
      "status": "deferred"
    }
  ]
}
